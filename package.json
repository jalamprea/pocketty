{
  "name": "tui-app-server",
  "version": "0.1.0",
  "private": true,
  "description": "Terminal UI server and PWA to manage remote terminal/tmux sessions from your phone.",
  "workspaces": [
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "postinstall": "node scripts/fix-node-pty-perms.mjs",
    "dev": "npm run dev --workspace=packages/server & npm run dev --workspace=packages/web",
    "dev:server": "npm run dev --workspace=packages/server",
    "dev:web": "npm run dev --workspace=packages/web",
    "build": "npm run build --workspace=packages/web && npm run build --workspace=packages/server",
    "start": "npm run start --workspace=packages/server",
    "hash": "npm run hash --workspace=packages/server"
  },
  "engines": {
    "node": ">=20"
  }
}
